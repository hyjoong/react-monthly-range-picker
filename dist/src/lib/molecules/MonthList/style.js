import { styled } from "../../../../stitches.config";
export var StyledMonthList = styled("div", {
    display: "grid",
    gridTemplateColumns: "repeat(3, 1fr)",
    width: "250px",
    gap: "4px",
});
